# Client (React + Vite)

## Scripts
- `npm run dev` — start dev server
- `npm run build` — production build
- `npm run preview` — preview production build

## Env
Create `.env` with:

```
VITE_API_BASE_URL=http://localhost:5000
```

On Vercel, set `VITE_API_BASE_URL` to your Render server URL.
